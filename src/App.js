import logo from './logo.svg';
import './App.css';
import Transaction from `./src/transcation.js`;
function App() {
  return (
    <div className="App">
      <header className="App-header">
        <img src={logo} className="App-logo" alt="logo" />
        const express = require('express');
const app = express();
const mongoose = require('mongoose');

mongoose.connect('mongodb://localhost/kirana-store-db', { useNewUrlParser:true, useUnifiedTopology: true });

      </header>
      <Transaction>

      </Transaction>
    </div>
  );
}
export default App;
